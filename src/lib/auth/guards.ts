/** @deprecated Import from `@/lib/auth/access` — kept for backward-compatible imports. */
export {
  ADMIN_ONLY_PATH_PREFIXES,
  EMPLEADO_PANEL_PATH_PREFIXES,
  assertAdminRole,
  assertPanelAccess,
  canAccessPanelRoute,
  isAdminOnlyPath,
  isKnownPanelRole,
} from "@/lib/auth/access";
